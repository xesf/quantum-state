const PI = Math.PI
const TAU = 2 * PI
const HALF_PI = PI / 2
const EPSILON = 1e-6
const DEG_TO_RAD = PI / 180
const RAD_TO_DEG = 180 / PI

export {
    PI,
    TAU,
    HALF_PI,
    EPSILON,
    DEG_TO_RAD,
    RAD_TO_DEG
}
